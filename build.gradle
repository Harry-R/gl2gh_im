apply plugin: 'java'

repositories {
	mavenCentral()
}

dependencies {
	compile 'org.json:json:20090211'
}

jar {
	from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
	manifest {
		attributes 'Main-Class': 'de.harry_r.gl2gh_im.Migrate'
	}
}
